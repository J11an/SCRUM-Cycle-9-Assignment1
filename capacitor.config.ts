import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SCRUM-Cycle-9-Assignment1',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
